/*import { easyStore as originalEasyStore } from 'react-easy-params'
import { appStores } from './stores'

export default function easyStore (store, config) {
  store = originalEasyStore(store, config)
  if (config) {
    appStores.add(store)
  }
  return store
}*/
